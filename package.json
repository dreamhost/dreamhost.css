{
  "name": "dreamhost-css",
  "version": "0.0.0-dev",
  "//": "     ^-....... Changing it will have not effect. Version number is managed by CI. See README.md.",
  "description": "DreamHost's CSS framework",
  "repository": {
    "type": "git",
    "url": "https://github.com/dreamhost/dreamhost.css.git"
  },
  "scripts": {
    "version": "echo 'Execute this from master branch to release a new version:'; echo 'git tag <version>; git push --tags'; echo; echo 'Example:'; echo 'git tag 1.2.3; git push --tags'",
    "dist": "gulp dist"
  },
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "gulp": "^4.0.2",
    "gulp-autoprefixer": "^3.1.1",
    "gulp-clean-css": "^4.2.0",
    "gulp-concat": "^2.6.1",
    "gulp-rename": "1.2.2",
    "gulp-sass": "^2.3.2",
    "gulp-uglify": "^2.0.1"
  },
  "license": "MIT",
  "dependencies": {
    "npm": "^6.11.3"
  }
}
